# Cocktails

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.2.7.

Uncomment the import of `seedData` and the `seedData()` function in `app.ts` so that the cocktails are sent to the database before using the program.

DB → https://restapi.fr/api/cocktails

WORK IN PROGRESS ...
